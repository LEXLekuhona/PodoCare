services:
  postgres:
    image: postgres:16-alpine
    container_name: podocare-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-podocare}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-podocare_dev_pwd}
      POSTGRES_DB: ${POSTGRES_DB:-podocare}
      POSTGRES_INITDB_ARGS: "--encoding=UTF-8 --locale=C"
      PGDATA: /var/lib/postgresql/data/pgdata
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-podocare} -d ${POSTGRES_DB:-podocare}"]
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - podocare

  redis:
    image: redis:7-alpine
    container_name: podocare-redis
    restart: unless-stopped
    # noeviction — как рекомендует BullMQ (не выкидывать ключи очередей при давлении по памяти).
    command: ["redis-server", "--appendonly", "yes", "--maxmemory-policy", "noeviction"]
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10
    networks:
      - podocare

  minio:
    image: minio/minio:latest
    container_name: podocare-minio
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${S3_ACCESS_KEY:-podocare}
      MINIO_ROOT_PASSWORD: ${S3_SECRET_KEY:-podocare_dev_pwd}
    ports:
      - "${S3_PORT:-9000}:9000"
      - "${S3_CONSOLE_PORT:-9001}:9001"
    volumes:
      - minio_data:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - podocare

  minio-setup:
    image: minio/mc:latest
    container_name: podocare-minio-setup
    depends_on:
      minio:
        condition: service_healthy
    entrypoint: >
      /bin/sh -c "
      mc alias set local http://minio:9000 ${S3_ACCESS_KEY:-podocare} ${S3_SECRET_KEY:-podocare_dev_pwd};
      mc mb --ignore-existing local/${S3_BUCKET:-podocare-dev};
      mc anonymous set download local/${S3_BUCKET:-podocare-dev};
      exit 0;
      "
    networks:
      - podocare

  adminer:
    image: adminer:latest
    container_name: podocare-adminer
    restart: unless-stopped
    ports:
      - "${ADMINER_PORT:-8080}:8080"
    environment:
      ADMINER_DEFAULT_SERVER: postgres
      ADMINER_DESIGN: pepa-linha-dark
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - podocare

volumes:
  postgres_data:
  redis_data:
  minio_data:

networks:
  podocare:
    driver: bridge

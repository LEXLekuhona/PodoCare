import { SpecialistsPage } from '@/pages/specialists/ui/SpecialistsPage';

/** Вкладка «Запись»: специалист → его услуги → дата и время. */
export default function BookingRoute() {
  return <SpecialistsPage />;
}


# Устарело как отдельный файл: те же переменные заданы в корневом `.env.example`
# (блок «Docker prod» и «VPS без домена»). Используйте корневой `.env` для compose.
#
# Кратко:
#   VITE_API_URL, API_CORS_ORIGINS, ADMIN_HTTP_PORT, RUN_DB_MIGRATIONS, VPS_PUBLIC_HOST
